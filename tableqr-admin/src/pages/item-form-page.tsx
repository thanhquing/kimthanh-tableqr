import { formatVnd, type CreateMenuItemRequest } from '@kimthanh-tableqr/contracts'
import { Button, ErrorState, LoadingSkeleton } from '@kimthanh-tableqr/ui'
import { useQuery } from '@tanstack/react-query'
import { useEffect, useMemo, useState } from 'react'
import { useNavigate, useParams } from 'react-router-dom'
import { useAdminAuth } from '../features/auth/auth-context'
import { createItem, getCategories, getItems, updateItem } from '../lib/api/admin'

type Form = { categoryId: string; name: string; description: string; price: string; imageUrl: string; available: boolean; sortOrder: string }
const empty: Form = { categoryId: '', name: '', description: '', price: '', imageUrl: '', available: true, sortOrder: '0' }
export function ItemFormPage() { const { auth } = useAdminAuth(); const { id } = useParams(); const navigate = useNavigate(); const [form, setForm] = useState<Form>(empty); const [errors, setErrors] = useState<Record<string, string>>({}); const [saving, setSaving] = useState(false); const [dirty, setDirty] = useState(false); const categories = useQuery({ queryKey:['admin-categories'], queryFn:()=>getCategories(auth!.token) }); const items = useQuery({ queryKey:['admin-items'], queryFn:()=>getItems(auth!.token) }); const item = useMemo(() => items.data?.items.find((candidate)=>candidate.id===id), [id, items.data]); useEffect(()=>{ if (!id || !item) return; setForm({ categoryId:item.categoryId,name:item.name,description:item.description??'',price:String(item.priceVnd),imageUrl:item.imageUrl??'',available:item.isAvailable,sortOrder:String(item.sortOrder) }) },[id,item]); useEffect(()=>{ const warn=(event:BeforeUnloadEvent)=>{if(dirty){event.preventDefault();event.returnValue=''}}; addEventListener('beforeunload',warn);return()=>removeEventListener('beforeunload',warn)},[dirty]); if(categories.isPending||items.isPending)return <div className="admin-form"><LoadingSkeleton height={440} width="100%"/></div>; if(categories.error||items.error)return <ErrorState action={<Button onClick={()=>window.location.reload()} variant="secondary">Thử lại</Button>} title="Không tải được món"/>; if(id&&!item)return <ErrorState action={<Button onClick={()=>navigate('/menu')} variant="secondary">Về thực đơn</Button>} title="Không tìm thấy món"/>; const change=(key:keyof Form,value:string|boolean)=>{setDirty(true);setForm(current=>({...current,[key]:value}));setErrors(current=>({...current,[key]:''}))}; async function save(event:React.FormEvent){event.preventDefault();const next:Record<string,string>={};if(!form.categoryId)next.categoryId='Chọn danh mục.';if(!form.name.trim())next.name='Nhập tên món.';const price=Number(form.price);if(!Number.isInteger(price)||price<0)next.price='Giá phải là số nguyên không âm.';if(Object.keys(next).length){setErrors(next);return}const body:CreateMenuItemRequest={categoryId:form.categoryId,name:form.name.trim(),description:form.description.trim()||null,priceVnd:price,imageUrl:form.imageUrl.trim()||null,sortOrder:Number(form.sortOrder)||0};setSaving(true);try{if(item)await updateItem(auth!.token,item.id,{...body,isAvailable:form.available});else await createItem(auth!.token,body);setDirty(false);navigate('/menu')}catch(caught){setErrors({form:caught instanceof Error?caught.message:'Không thể lưu món.'})}finally{setSaving(false)}} return <form className="admin-form" onSubmit={(event)=>void save(event)}><header><div><h1>{item?'Sửa món':'Thêm món mới'}</h1><p>Thông tin này hiển thị cho khách khi quét mã QR.</p></div></header>{errors.form?<p className="admin-menu__error">{errors.form}</p>:null}<label>Danh mục<select onChange={(e)=>change('categoryId',e.target.value)} value={form.categoryId}><option value="">Chọn danh mục</option>{categories.data!.categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select>{errors.categoryId?<small>{errors.categoryId}</small>:null}</label><label>Tên món<input onChange={(e)=>change('name',e.target.value)} value={form.name}/>{errors.name?<small>{errors.name}</small>:null}</label><label>Mô tả<textarea onChange={(e)=>change('description',e.target.value)} value={form.description}/></label><label>Giá (VNĐ)<input inputMode="numeric" onChange={(e)=>change('price',e.target.value)} value={form.price}/><em>Hiển thị cho khách: {form.price&&Number.isFinite(Number(form.price))?formatVnd(Number(form.price)):'-'}</em>{errors.price?<small>{errors.price}</small>:null}</label><label>URL ảnh<input onChange={(e)=>change('imageUrl',e.target.value)} value={form.imageUrl}/></label><label>Thứ tự<input inputMode="numeric" onChange={(e)=>change('sortOrder',e.target.value)} value={form.sortOrder}/></label><label className="admin-form__check"><input checked={form.available} onChange={(e)=>change('available',e.target.checked)} type="checkbox"/>Còn hàng</label><footer><Button onClick={()=>navigate('/menu')} type="button" variant="secondary">Hủy</Button><Button disabled={saving} type="submit">{saving?'Đang lưu...':'Lưu món'}</Button></footer></form> }
